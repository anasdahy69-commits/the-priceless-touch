name: Optimize Images

on:
  push:
    paths:
      - 'images/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Compress Images
        uses: calibre/image-actions@main
        with:
          githubToken: ${{ secrets.GITHUB_TOKEN }}
